import { calculateCircumference } from './math/circle';
import { rectangleCircumference } from './math/rectangle';

console.log(calculateCircumference(10));
console.log(rectangleCircumference(10, 20));
